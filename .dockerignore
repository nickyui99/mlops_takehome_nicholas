# Keep secrets and dev artifacts out of Docker build context
.env
*.env
.env.*

# Python cache and venv
__pycache__/
*.pyc
.venv/

# MLflow and local artifacts
mlruns/
mlflow.db
.trash/

# Airflow data and logs (not needed in production container)
airflow-data/

# Git & editor files
.git/
.gitignore
.github/
.vscode/
.idea/

# Logs
*.log

# Test files and documentation (not needed in production)
tests/
*.md
dashboards/
deploy/

# Docker compose files (not needed inside container)
docker-compose*.yaml
Dockerfile

# Airflow pipelines (only needed in Airflow, not in API container)
pipelines/

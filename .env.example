# Example environment configuration
# Copy this file to `.env` and fill in real values. Never commit `.env`.

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mlops
DB_USER=postgres
DB_PASSWORD=change_me

# Model serving
MODEL_NAME=iris-classifier
MODEL_VERSION=1
MODEL_URI=artifacts/iris-classifier

# Training flags
REGISTER_TO_MLFLOW=false

# Kubernetes/runtime (usually auto-populated)
# HOSTNAME=local
